<h3>Plan page</h3>

<div class="row">
    <div class="col-md-12">
        <div class="btn-toolbar">
            <div class="btn-group">
                <a class="btn btn-success" (click)="addRepast(0)">Добавить</a>
                <a class="btn btn-success" (click)="addRepast(1)">Завтрак</a>
                <a class="btn btn-success" (click)="addRepast(2)">Обед</a>
                <a class="btn btn-success" (click)="addRepast(3)">Ужин</a>
                <a class="btn btn-success" (click)="addRepast(4)">Перекус</a>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
<<<<<<< Updated upstream
          <table class="table table-hover suplan-table">
              <thead>
                  <tr>
                      <th>Название</th>
                      <th>Вес</th>
                      <th>Калорийность</th>
                  </tr>
              </thead>
              <tbody>
                  <tr *ngFor="let row of repasts" routerLink="/plan/{{dayIndex}}/{{row.index}}">
                      <td>{{row.name}}</td>
                      <td>{{row.weight}}</td>
                      <td>{{row.calories}}</td>
                  </tr>
              </tbody>
=======
        <div class="mat-elevation-z8">
          <table class="table table-hover ">
            <thead>
              <tr>
                <th>Наименование</th>
                <th>Вес</th>
                <th>Калорийность</th>
              </tr>
            </thead>
            <tbody>
              <tr *ngFor="let row of repasts">
                <td>
                    <a routerLink="/plan/{{dayIndex}}/{{row.index}}">
                        {{row.name}}
                    </a>
                </td>
                <td>{{row.weight}}</td>
                <td>{{row.calories}}</td>
                <td>
                    <i class="fa fa-trash" aria-hidden="true" (click)="removeRepast(row.index)"></i>
                </td>
              </tr>
            </tbody>
>>>>>>> Stashed changes
          </table>
    </div>
</div>
